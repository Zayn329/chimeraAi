# 1. NEVER upload your API keys!
.env

# 2. NEVER upload the Python virtual environment (it has thousands of files)
.venv/
venv/
env/

# 3. NEVER upload your Vector Databases (These are huge and should stay local)
storage/

# 4. Standard Python Cache garbage
__pycache__/
*.pyc

# 5. Frontend dependencies (never commit node_modules)
frontend/node_modules/
frontend/.npm
frontend/dist/
frontend/build/